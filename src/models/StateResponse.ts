export interface StateResponse {
    id: string;
    name: string;
    code: string;
    countryId: string;
    countryName: string;
    isActive: boolean;
    createdAt: string;
    updatedAt: string;
}
