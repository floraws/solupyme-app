export interface CityResponse {
    id: string;
    name: string;
    code: string;
    stateId: string;
    stateName: string;
    countryName: string;
    isActive: boolean;
    createdAt: string;
    updatedAt: string;
}
